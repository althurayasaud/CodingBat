public Map<String, Boolean> wordMultiple(String[] strings) {
    Map map = new HashMap();
  for(String s : strings)
  {
    if(!map.containsKey(s)) 
    {
      map.put(s, false);
    }
    else
    {
      map.put(s, true);
    }
  }
  return map;
}
